<% provide(title:"HOME") %>
<section id="todo" class="todo bg-dark">
  <div class="container">
    <%= render partial: 'post_up', locals: { task: @task } %>
    <div class="lists">
     <%= render @tasks %>
    </div>
  </div>
</section> 