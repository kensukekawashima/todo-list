<% provide(:button_text, 'UPDATE') %>
<div class="todo bg-dark text-light text-center">
  <h2 class="p-4 m-4">EDIT</h2>
  <%= render 'form' %>
  <div class="d-flex justify-content-center form-group w-50 mx-auto mt-4">
    <% if current_user?(@user) %>
      <span>
        <%= link_to user_path(@user), class:"btn btn-danger rounded-circle mx-2", method: :delete, data: { confirm: "You sure?" } do %>
          <%= fa_icon('trash-alt') %>
        <% end %>
      </span>  
    <% end %>
  </div>
</div>
